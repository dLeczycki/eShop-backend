export interface Image {
  name: string;
  url: string;
  productId: string;
}

export enum AlloweImageExtensions {
  JPG = '.jpg',
  JPEG = '.jpeg',
  PNG = '.png',
  ICO = '.ico',
  SVG = '.svg',
  WEBP = '.webp',
}